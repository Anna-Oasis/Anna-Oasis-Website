$kind: http-request
url: "{{BASE_URL}}/api/manager/grievance/:grievanceId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  grievanceId: "1"
auth:
  type: bearer
  credentials:
    token: "{{MANAGER_TOKEN}}"
order: 2000
